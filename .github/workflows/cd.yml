on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}

    steps:
      - name: Check Out Code
        uses: actions/checkout@v4
      
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.0"
      
      - name: Build Prod
        run: ./scripts/buildprod.sh

       
      - id: 'auth'
        name: Setup Google Cloud
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: 'Set up Cloud SDK'
        uses: 'google-github-actions/setup-gcloud@v3'

      - name: 'Use gcloud CLI'
        run: 'gcloud info'
      
      - name: Push Docker Image to Google Artifact Registry
        run: gcloud builds submit --tag us-central1-docker.pkg.dev/notely-499006/notely-ar-repo/notely:latest .

      - name: Install Goose Runner
        run: go install github.com/pressly/goose/v3/cmd/goose@latest
      
      - name: Database Migration
        run: ./scripts/migrateup.sh
      
      - name: Deploy to Cloud Run
        run: gcloud run deploy notely --image us-central1-docker.pkg.dev/notely-499006/notely-ar-repo/notely:latest --region us-central1 --allow-unauthenticated --project notely-499006 --max-instances=4   